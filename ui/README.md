# FIX Hub UI (minimal)

This is a minimal Vite + React + TypeScript UI for the FIX Hub prototype.

Run locally:

```bash
cd ui
npm install
npm run dev
```

The dev server proxies `/api` to `http://localhost:8080`.

Next steps:

- Add editing of routes and config
- Add WebSocket streaming for message flow
- Improve sessions display and action buttons
